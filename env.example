# JWT секретные ключи (ОБЯЗАТЕЛЬНО измените на production!)
# Сгенерируйте безопасные ключи: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production

# Время жизни токенов
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# Порт сервера
PORT=3001

# Хост сервера (для формирования URL в development)
HOST=localhost

# Публичный URL API (для production обязательно!)
# Используется для генерации URL к загруженным файлам
API_URL=http://XXX.XXX.XXX.XXX:3001

# Альтернативная переменная для публичного URL (если отличается от API_URL)
# PUBLIC_URL=http://XXX.XXX.XXX.XXX:3001

# Пути к данным (опционально, по умолчанию используются пути в проекте)
# Если нужно хранить данные вне проекта, укажите эти переменные
# DB_PATH=/path/to/database
# UPLOADS_PATH=/path/to/uploads

# Окружение
NODE_ENV=production

# CORS настройка (опционально, по умолчанию разрешены все домены)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Rate limiting (опционально)
# RATE_LIMIT_MAX=100          # Максимум запросов за 15 минут (по умолчанию 100)
# AUTH_RATE_LIMIT_MAX=5       # Максимум попыток авторизации за 15 минут (по умолчанию 5)

